{{- if .Values.tibco-otel-collector-finops.enabled -}}
apiVersion: opentelemetry.io/v1alpha1
kind: OpenTelemetryCollector
metadata:
  name: {{ .Values.tibco-otel-collector-finops.name }}
  namespace: {{ .Release.Namespace }}
spec:
  mode: "{{ .Values.tibco-otel-collector-finops.mode }}"
  ports:
  {{- range $key, $port := .Values.tibco-otel-collector-finops.ports }}
  {{- if $port.enabled }}
    - name: {{ $key }}
      targetPort: {{ $port.targetPort }}
      port: {{ $port.port }}
      protocol: {{ $port.protocol }}
  {{- end }}
  {{- end }}
  config: |
{{- if .Values.tibco-otel-collector-finops.config }}
{{ .Values.tibco-otel-collector-finops.config | indent 4 }}
{{- else }}
{{ tpl (.Files.Get "config/tibco-otel-collector-finops.yaml") . | indent 4 }}
{{- end }}
{{- end }}